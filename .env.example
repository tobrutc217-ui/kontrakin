# Supabase configuration (optional)
# If provided, the app will connect to your live database.
# If omitted or left empty, the app falls back to a high-fidelity local-storage mock database automatically.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
